version: "3.8"

services:
  server:
    build:
      context: ./server
    ports:
      - "9999:9999"
    container_name: server

  client:
    build:
      context: ./client
    depends_on:
      - server
    container_name: client
